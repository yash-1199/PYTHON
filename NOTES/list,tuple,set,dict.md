int
float
string
boolean

list:
    Store marks of Ram 6 subjects
    list: will have that elemets and also there indexes

    Ram=[80,76,90,86,79,76] 
    name=['Telugu','maths','science']
    l=['ram',100,52.565,False]

    Ram[3]
    location=[17.582,88.6585]
    location[0]=10
    ticket=['Ravi',12585,'S3']

    List: that stores multiple values in as a single variable
    #once I store the values inside my data striucture it should not change

tuple:
ticket=('Ravi',12585,'S3')
list:
   Mutable object
tuple:
    immutable object=we cananot change the elements inside the tuple
    tuple is immutabe ,we cant change element but you can assign a new tuple to the variable
    python has not changed the tuple,instead it created new tuple and the pointer t 
    is pointing to new tuple
    print(id(t))


set:I stores only unique values
    I have a website I want to store the user name who are visiting website
    Contraint : a single user can visit multiple time
    diff b/w list and set is list can store duplicates set cant store duplicates
    l=('Ram','Sita','ramya','Raj','Ram','Sita')
dict:
    Ram=[80,76,90,86,79,76] 
    d={"maths":80,'science':97,'social':88}
    It stors value in key:value pair


1.List: if I want to store multiple values in one variable we use list
2.Tuple:if I want to store multiple values in one variable we can use tuple diff is list is     a mutable object and tuple is a immutable object
If I dont want to change any value that  stored then I goo for tuple instead of list

3.set: set is same like list but diff is it doest store duplicate values
4.dict: dict is used to store values in key value pair


